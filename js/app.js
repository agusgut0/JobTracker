/**
 * APP — Bootstrapper. Entry point.
 * Imports the Controller and fires init() when the DOM is ready.
 */

import { init } from './controller.js';

document.addEventListener('DOMContentLoaded', init);
